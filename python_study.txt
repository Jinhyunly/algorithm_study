------------------------------------------------------
- rstrip() 라이브러리

import sys

sys.stdin.readline().rstrip()
개행문자 삭제

------------------------------------------------------
stack = []

if not stack  #스텍에 값이 존재하지 않는다
  pirnt("empty")

------------------------------------------------------
